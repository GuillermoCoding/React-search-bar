import React from 'react';
import ReactDOM from 'react-dom';

const Root = ()=>{
    return (
        <div>
            SearchBar
        </div>
    );
};

ReactDOM.render(
    <Root/>,
    document.getElementById('root')
);